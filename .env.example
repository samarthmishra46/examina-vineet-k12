# Step 2 — Auth (Google OAuth via Auth.js v5)
# Generate AUTH_SECRET with: openssl rand -base64 32
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Step 3 — MongoDB (Atlas in prod, local Mongo for dev is also fine)
MONGODB_URI=

# Step 4 — Admin (chapter generation + PDF storage)
ANTHROPIC_API_KEY=
BLOB_READ_WRITE_TOKEN=

# Step 7 — Text-to-speech
OPENAI_API_KEY=
